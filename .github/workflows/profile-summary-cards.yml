name: Profile Summary Cards

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Use FEINA_TOKEN
      env:
        GITHUB_TOKEN: ${{ secrets.FEINA_TOKEN }}
      run: echo "Using FEINA_TOKEN"